"""AI provider abstractions.

This sub-package defines the contract that any AI model provider (e.g. an
LLM vendor) must implement. No concrete provider (such as OpenAI) is
implemented here — only the abstract interface lives in this package.
"""
