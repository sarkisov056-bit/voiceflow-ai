"""Conversation management sub-package.

Contains the data model for a single conversation message
(``ConversationMessage``) and the ``ConversationService`` responsible for
orchestrating conversation flow.
"""
